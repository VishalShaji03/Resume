name: Deploy Backend
on:
  push:
    branches: [main]
    paths: ['compute/**', 'Dockerfile']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and SOCI Index
        run: |
          docker build -t phantom-backend .
          # (Standard push and soci create/push steps here)